#include<stdio.h>
int main()
{
int num,tnum,count;
scanf("%d",&num);
count=o;
tnum=num;
while(tnum>0)
{
count++;
tnum/=10;
}
printf("total number of digit are:%d in number: %d,',count,num);
return 0;

}
